export const startGameSwitcherSelector = (state) => {
    return state.startGameSwitcher;
}

export const userDataSelector = (state) => {
    return state.userData;
}

export const counterSelector = (state) => {
    return state.counter;
}